{
   "name": "resolvendo-problemas",
   "version": "1.0.0",
   "main": "index.js",
   "scripts": {
      "hash-tables": "clear && tsnd --transpile-only --ignore-watch node_modules src/hash_tables",
      "exercise1": "clear && tsnd --transpile-only --ignore-watch node_modules src/exercise1",
      "exercise2": "clear && tsnd --transpile-only --ignore-watch node_modules src/exercise2"
   },
   "license": "ISC",
   "devDependencies": {
      "@types/node": "^15.3.0",
      "ts-node-dev": "^1.1.6",
      "typescript": "^4.2.4"
   }
}
